

function generateHTML({
  locale, // so important.... process.env?
  title,
  description,
  author,
  stylesheets, // function, or result?
  scripts, // function or result?
  runtimeManifest,
  chunkmanifest,
  preloadedState,
}) {
  return undefined
}